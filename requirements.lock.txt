# ForgeOS API — pinned dependency lock.
#
# Generated from a known-good test environment (Debian 13, Python 3.13,
# full suite: 194 + 11 backup tests passing). Reproduces the exact set of
# packages used by CI and the in-VM test run.
#
# Why this exists: pyproject.toml keeps LOOSE version ranges. A fresh
# `pip install -e .` can resolve an incompatible bcrypt (5.x, which breaks
# passlib's hash/verify -> nobody can log in) or a Starlette whose path
# handling changes API behaviour. This file pins the resolved set so the
# suite is reproducible and cannot silently regress.
#
# Usage (recommended for CI / clean machines):
#   python -m venv .venv && . .venv/bin/activate
#   pip install -e .
#   pip install -r requirements.lock.txt
#
# The project's own package (forgeos-api) is intentionally NOT pinned here
# (it is installed editable from this repo). Runtime + optional deps
# (slowapi for rate limiting, boto3 for RustFS/S3) are included.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
bcrypt==4.0.1
boto3==1.43.46
botocore==1.43.46
certifi==2026.6.17
click==8.4.2
Deprecated==1.3.1
ecdsa==0.19.2
fastapi==0.139.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
jmespath==1.1.0
limits==5.8.0
packaging==26.2
passlib==1.7.4
pluggy==1.6.0
psutil==7.2.2
pyasn1==0.6.4
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
PyOTP==2.10.0
pytest==9.1.1
pytest-asyncio==1.4.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-jose==3.5.0
python-multipart==0.0.32
PyYAML==6.0.3
rsa==4.9.1
s3transfer==0.19.1
six==1.17.0
slowapi==0.1.10
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.51.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1
wrapt==2.2.0
